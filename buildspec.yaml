version: 0.2
phases:
  install:
    commands:
      - echo "[+] Hello"

    pre_build:
      commands:
        - echo "[+] Running tests..."

      build:
        commands:
          - pip install -r requirements.txt
      
        post_build:
          commands:
            - echo "Packaging Completed on `data`"

            artifacts:
              files:
